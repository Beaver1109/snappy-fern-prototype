/**
 * Ambient module for template authoring when `@thryvlabs/dex-react` is not installed.
 * In generated prototypes, types resolve from `node_modules` as usual.
 */
declare module '@thryvlabs/dex-react';
